import { TemplateRendererDirective } from './template-renderer.directive';

describe('TemplateRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateRendererDirective();
    expect(directive).toBeTruthy();
  });
});
